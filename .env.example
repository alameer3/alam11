# ملف البيئة المثال لمشروع YEMEN FLIX
# انسخ هذا الملف إلى .env وأضف القيم الصحيحة

# قاعدة البيانات (اختياري)
# إذا لم تتوفر قاعدة بيانات، سيعمل المشروع بالذاكرة المؤقتة
DATABASE_URL=postgresql://user:password@localhost:5432/database

# إعدادات الخادم
NODE_ENV=development
PORT=5000

# إعدادات الأمان (اختياري)
JWT_SECRET=your-jwt-secret-key-here
ADMIN_PASSWORD=admin123

# بيانات دخول الإدارة (للتطوير فقط)
VITE_ADMIN_USERNAME=admin
VITE_ADMIN_PASSWORD=cinema@2024

# إعدادات الذكاء الاصطناعي (اختياري)
OPENAI_API_KEY=your-openai-api-key-here

# إعدادات الإيميل (اختياري)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-password

# إعدادات الرفع (اختياري)
UPLOAD_MAX_SIZE=10MB
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/gif,video/mp4

# إعدادات Redis (اختياري)
REDIS_URL=redis://localhost:6379

# إعدادات التحليلات (اختياري)
ANALYTICS_ENABLED=true
GOOGLE_ANALYTICS_ID=your-analytics-id